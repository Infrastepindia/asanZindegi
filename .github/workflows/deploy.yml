name: Build and Deploy (SSR)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build Angular SSR app
        run: npm run build -- --configuration production
        
      - name: Show dist folder (debug)
        run: ls -R dist/asanZindegi

      - name: Upload build artifact (SSR)
        uses: actions/upload-artifact@v4
        with:
          name: dist-ssr
          path: dist/asanZindegi

      - name: Upload package files
        uses: actions/upload-artifact@v4
        with:
          name: package-files
          path: |
            package.json
            package-lock.json

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist-ssr
          path: dist-ssr

      - name: Download package files
        uses: actions/download-artifact@v4
        with:
          name: package-files
          path: package-files

      - name: Show downloaded files (debug)
        run: ls -R

      - name: Deploy dist (SSR) to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.ASANZINDEGI_HOST }}
          username: ${{ secrets.ASANZINDEGI_USER }}
          password: ${{ secrets.ASANZINDEGI_PASSWORD }}
          local-dir: dist-ssr/
          server-dir: /home8/hirenqco/asanzindegi/
          dangerous-clean-slate: true

      - name: Deploy package.json to cPanel root
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.ASANZINDEGI_HOST }}
          username: ${{ secrets.ASANZINDEGI_USER }}
          password: ${{ secrets.ASANZINDEGI_PASSWORD }}
          local-dir: package-files/
          server-dir: /home8/hirenqco/asanzindegi/
          dangerous-clean-slate: false
