name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-browser
          path: dist/asanZindegi/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist-browser
          path: dist

      - name: Deploy to server via SSH (rsync)
        uses: easingthemes/ssh-deploy@v4
        env:
          SSH_PRIVATE_KEY: ${{ secrets.ASANZINDEGI_SECRET_KEY }}
          REMOTE_HOST: ${{ secrets.ASANZINDEGI_HOST }}
          REMOTE_USER: ${{ secrets.ASANZINDEGI_USER }}
          REMOTE_PORT: ${{ secrets.SSH_PORT || 22 }}
          ARGS: '-avzr --delete'
          SOURCE: 'dist/asanZindegi/browser/'
          TARGET: '/home/asanZindegi/'
